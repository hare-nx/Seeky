<%= @comments.each do |comment| %>
  <%= link_to post_path(comment.post) do %>
    @<%= comment.post.user_id %>
  <% end %>
  <%= comment %>
  <%= comment.created_at.strftime("%Y.%m.%d   %H:%M:%S") %>
  <%= link_to "削除", post_comment_path(comment) %>
<% end %>


