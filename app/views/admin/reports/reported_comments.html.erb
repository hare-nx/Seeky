<div class="container-fruid p-5 m-5">
  <div class="row mb-5">
    <h4 class="offset-sm-2">コメント通報履歴</h4>
  </div>
  <div class="mb-5 justify-content-center">
    <table class="table col-sm-8 mx-auto table-hover text-dark">
      <thead class="thead-light">
        <tr>
          <th>コメント投稿日</th>
          <th>ユーザーID</th>
          <th>通報数</th>
          <th>ステータス</th>
          <th></th>
        </tr>
      </thead>
      <% @comments.each do |comment| %>
        <tr>
          <td>
            <%= link_to admin_comment_path(comment), class: "text-decoration-none text-dark" do %>
              <%= comment.created_at.strftime("%Y/%m/%d %H:%M") %>
            <% end %>
          </td>
          <td>
            <%= link_to admin_user_path(comment.user), class: "text-decoration-none text-dark" do %>
              <%= comment.user_id %>
            <% end %>
          </td>
          <td><%= comment.user.number_of_reported_comments(comment.user) %></td>
          <td>
            <% if comment.status? %>
              <span class="font-weight-bold text-success">表示</span>
            <% else %>
              <span class="font-weight-bold text-muted">非表示</span>
            <% end %>
          </td>
          <td><%= button_to "対象の投稿詳細", admin_post_path(comment.post), class: "btn btn-outline-info btn-sm" %></td>
        </tr>
      <% end %>
    </table>
  </div>
</div>